# AI authoring workflows

Use `manage_ai` for Behavior Trees, Blackboard assets, EQS, navigation, perception, and State
Trees. Read the current asset structure before editing, use explicit asset paths, bind Blackboard
keys by name, compile/save after mutation, and run a bounded runtime validation when a world is
available.
