# Animation authoring workflows

Use `animation_physics` for animation sequences, bone tracks, keyframes, curves, notifies,
montages, blend spaces, and animation-blueprint state machines. Validate skeleton and frame-rate
assumptions first, keep frame indices non-negative, compile or refresh dependent assets, and
verify saved animation metadata after mutation.
