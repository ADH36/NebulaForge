# NebulaForge workflow skills

Use the MCP `list_skills` action to discover installed workflow packs and `get_skills` to load only
the packs needed for the current task. Installed VibeUE packs under `Plugins/VibeUE/Content/Skills`
are discovered automatically and take precedence over duplicate local names.
