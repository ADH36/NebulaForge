# map-blockout

Create rapid level blockouts with reusable primitives, layout constraints, and named gameplay spaces.

Keep blockout operations deterministic, save the level explicitly, and verify actor counts and bounds.
