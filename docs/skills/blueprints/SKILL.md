# Blueprint authoring workflows

Inspect the Blueprint class and existing graphs before editing. Use `manage_blueprint` for graph,
pin, component, interface, event-dispatcher, and compile operations. Batch related graph edits,
compile after mutation, save through the bridge, and return the asset path plus verification data.
