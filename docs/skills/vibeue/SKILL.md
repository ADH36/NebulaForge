# vibeue

Use the VibeUE compatibility surface: discover installed service classes, load the relevant skill pack, and call service methods through `call_vibeue_service`.

VibeUE services are optional. Confirm availability with `discover_python_class` or `list_python_subsystems`; do not assume a service exists on UE versions without the VibeUE plugin.
