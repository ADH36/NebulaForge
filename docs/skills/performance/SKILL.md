# Performance and profiling workflows

Start with `system_control.capture_insights_trace` or `manage_performance` to capture a bounded
trace, reproduce the workload, stop the capture, and analyze the result. Record the target map,
platform, scalability settings, and trace path. Prefer dry-run and validation actions before
launching packaged builds or external profiling tools.
